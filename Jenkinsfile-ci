stage ('Scan and Build Jar File') {
            steps {
               withSonarQubeEnv(installationName: 'SonarServer-jenkins', credentialsId: 'SonarQubeToken') {
                sh 'mvn clean package sonar:sonar'
                }
            }
        }
